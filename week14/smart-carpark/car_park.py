# Carpark
# Car
# Sensor
# Display
class CarPark:
    """
    Consolidates cars within a defined parking area (aka. carpark)
    """
    def __init__(self,
                 name,
                 location,
                 max_bays,
                 occupied = 0,
                 displays = None,
                 cars = None):
        ...
        # that's a lot of attributes!

